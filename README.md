# route-tag-shadow
Provide tag "shadow". Easy to use tag that set "shadow" state on the endpoint


Example config: 
```
{
  tag: "shadow",
  config: {
    disable: true,
    voteSize: 1000,
  },
  handler: "route-tag-shadow"
}
```
